<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:background="@drawable/bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/BoldStyle"
            android:layout_marginTop="16dp"
            android:layout_gravity="center"
            android:text="@string/explore_music"
            android:textSize="24sp" />

        <TextView
            android:id="@+id/textSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/RegularStyle"
            tools:text="Loading your audio library..."
            android:textSize="14sp"
            android:layout_marginTop="4dp" />
    </LinearLayout>

    <Button
        android:id="@+id/buttonGrantPermission"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/grant_audio_permission"
        android:visibility="gone"
        android:layout_margin="12dp" />

    <ProgressBar
        android:id="@+id/progressLoading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:indeterminateTint="@color/purple"
        android:layout_marginTop="16dp" />

    <TextView
        android:id="@+id/textEmpty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/RegularStyle"
        android:text="@string/no_audio_found_make_sure_there_are_music_files_on_your_device_and_permission_is_granted"
        android:textAlignment="center"
        android:textColor="#555555"
        android:padding="24dp"
        android:visibility="gone" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvAudio"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        tools:listitem="@layout/item_audio"
        tools:itemCount="3"/>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardElevation="8dp"
        app:cardCornerRadius="16dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/playerPanel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="15dp"
            android:background="@drawable/audio_item_blue_bg">

            <TextView
                android:id="@+id/textNowPlaying"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                style="@style/SemiBoldStyle"
                android:text="Nothing playing"
                android:singleLine="true"
                android:ellipsize="end"
                android:layout_marginEnd="12dp"
                app:layout_constraintEnd_toStartOf="@id/buttonToggle"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"/>

            <TextView
                android:id="@+id/textPlaybackState"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                style="@style/RegularStyle"
                android:text="Select a song"
                android:textSize="13sp"
                android:layout_marginTop="2dp"
                android:singleLine="true"
                android:ellipsize="end"
                android:layout_marginBottom="4dp"
                android:layout_marginEnd="12dp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toStartOf="@id/buttonToggle"
                app:layout_constraintTop_toBottomOf="@+id/textNowPlaying"
                app:layout_constraintStart_toStartOf="@id/textNowPlaying"/>

            <ImageView
                android:id="@+id/buttonToggle"
                android:layout_width="35dp"
                android:layout_height="35dp"
                tools:src="@drawable/ic_play_3"
                android:contentDescription="@string/img"
                android:layout_marginEnd="8dp"
                app:layout_constraintEnd_toStartOf="@id/buttonNext"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"/>

            <ImageView
                android:id="@+id/buttonNext"
                android:layout_width="35dp"
                android:layout_height="35dp"
                android:src="@drawable/ic_play_next"
                android:contentDescription="@string/img"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"/>

        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.cardview.widget.CardView>
</LinearLayout>
